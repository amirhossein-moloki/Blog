# سند جامع طراحی فنی و معماری سیستم محتوای بلاک‌محور پویا (Dynamic Block-Based Content Architecture)

**کد پروژه:** CMS-BLOCK-DYNAMIC-01
**نسخه سند:** 1.0.0-draft
**نویسنده:** Senior Django Architect & CMS Solution Architect
**مخاطبان:** تیم فنی، توسعه‌دهندگان بک‌اِند، توسعه‌دهندگان فرانت‌اند، مهندسان DevOps و تیم ممیزی معماری

---

## مقدمه (Introduction)

این سند معماری و طراحی فنی سیستم **Dynamic Block-Based Content** (محتوای پویا مبتنی بر بلاک) را به صورت جامع تشریح می‌کند. بر اساس تحلیل ساختار فعلی پروژه، معماری چندزبانی (Multilingual) و سیستم مدیریت رسانه پیشرفته مستقر، این مستند فنی به جایگاه، شیوه اعتبارسنجی و تعامل بلاک‌ها با لایه‌های گوناگون سیستم می‌پردازد.

مطابق الزامات دقیق و صریح کارفرما، این مستند فنی سطح بالا (Technical Architecture & Design Specification) منطبق با اصول **Clean Architecture** و استانداردهای **Enterprise** تهیه شده است.

---

## ۱. نمای کلی معماری (Architecture Overview)

سیستم محتوای سنتی با استفاده از فیلدهای Rich Text صِرف (مانند CKEditor 5 روی فیلد `content`) برای خروجی‌های مدرن که نیازمند چیدمان‌های گوناگون چندرسانه‌ای، کامپوننت‌های تعاملی پویا و بهینه‌سازی‌های سئو هستند، دیگر پاسخگو نیست. معماری **Block-Based Content** به نویسندگان اجازه می‌دهد مقالاتی با چیدمان آزاد متشکل از بلاک‌های متوالی (متن، تصویر، ویدیو، نقل‌قول، جداول و غیره) ایجاد کنند.

```
+------------------------------------------------------------+
|                       Article                              |
+------------------------------------------------------------+
                               |
                               | 1 : N (Translations)
                               v
+------------------------------------------------------------+
|                  ArticleTranslation                        |
|   - language_code: "fa" / "en"                             |
|   - content_blocks: JSONField                              | <---+ [معماری پیشنهادی]
+------------------------------------------------------------+
                               |
            +------------------+------------------+
            | 1:N (Nested references)             | 1:N (Media)
            v                                     v
+----------------------+               +---------------------+
| Dynamic JSON Blocks  |               |    medias.Media     |
| - Text, Media, Quote | <-----------  | - storage_key, url  |
+----------------------+  Ref ID       +---------------------+
```

### دلیل انتخاب JSONField در مدل `ArticleTranslation`
1. **پشتیبانی کامل از بومی‌سازی (Multi-language Integration):** با تعبیه فیلد `content_blocks` از نوع `JSONField` درون مدل موجود `ArticleTranslation`، ساختار بلاک‌ها به ازای هر زبان کاملاً مستقل و بومی‌سازی‌شده ذخیره می‌شود. بلاک‌های زبان فارسی می‌توانند ترتیب، محتوا و حتی رسانه‌های کاملاً متفاوتی نسبت به بلاک‌های زبان انگلیسی داشته باشند.
2. **انعطاف‌پذیری فوق‌العاده در ساختار (Schema Flexibility):** فرانت‌اند مدرن (Next.js/React) نیازمند ساختار درختی و قابل همگام‌سازی است. `JSONField` در PostgreSQL اجازه می‌دهد تا بدون نیاز به مهاجرت‌های مداوم دیتابیس (Schema Migrations)، در آینده انواع جدیدی از بلاک‌ها (مانند Accordion یا Related Articles) تعریف و بکارگیری شوند.
3. **عملکرد خواندن بهینه (Single-Query Fetch):** بر خلاف معماری رابطه‌ای (Relational Sections) که برای دریافت یک مقاله با ۱۰ بلاک نیاز به Joinهای متعدد و پرهزینه دارد، با `JSONField` کل مقاله به همراه تمام بلاک‌هایش در **یک کوئری تک‌جدولی ساده** واکشی شده و هزینه لایه‌بندی کاهش می‌یابد.

### نحوه تعامل با Media
سیستم مدیریت رسانه موجود پروژه بر پایه مدل `medias.Media` بنا شده است. در معماری بلاک‌محور:
- هر بلاک چندرسانه‌ای به جای ذخیره آدرس مستقیم هاردکد شده فایل، **شناسه عددی (Media ID)** مدل `medias.Media` را ذخیره می‌کند.
- لایه سریالایزر در زمان پاسخ‌دهی (Representation Flow)، این شناسه‌ها را به صورت پویا منبسط کرده و اطلاعات کامل رسانه (شامل URLهای بهینه‌شده، عرض، ارتفاع و تگ Alt) را جایگزین می‌کند.

### نحوه تعامل با API و Frontend
- **دریافت (Retrieve):** کلاینت درخواست مقاله را به همراه پارامتر `lang` ارسال می‌کند. سریالایزر بلاک‌ها را از ستون JSON واکشی کرده، رسانه‌ها را تزریق نموده و ساختار درختی مرتب‌شده‌ای را تحویل فرانت‌اند می‌دهد.
- **ارسال/به‌روزرسانی (Create/Update):** کلاینت آرایه‌ای ساختاریافته از بلاک‌ها را ارسال می‌کند. لایه سریالایزر علاوه بر اعتبارسنجی فیلدهای اصلی، بلاک‌ها را تک‌به‌تک بر اساس JSON Schema بررسی و ذخیره می‌کند.

---

## ۲. طراحی مدل داده (Data Model Design)

برای پیاده‌سازی این سیستم، فیلد `content` (نوع `CKEditor5Field`) در کلاس `ArticleTranslation` با یک فیلد مدرن به نام `content_blocks` از نوع `JSONField` جایگزین یا در کنار آن تکمیل می‌گردد.

### مشخصات فنی فیلد `content_blocks` در مدل `ArticleTranslation`:

| نام فیلد | نوع داده (Django) | قابلیت Null | Required | مقدار پیش‌فرض | قوانین اعتبارسنجی (Validation) | عملکرد و توصیف |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `content_blocks` | `models.JSONField` | بله | خیر | `list` | بررسی منطبق بر JSON Schema، عدم پذیرش ساختار نامعتبر | نگهداری آرایه‌ای منظم از تمام بخش‌ها و بلاک‌های تشکیل‌دهنده مقاله به ازای زبان جاری |

### نمونه ساختار داده درون JSON (Sample JSON Structure)

```json
[
  {
    "id": "blk_8f1c4e92_12ba",
    "type": "text",
    "order": 1,
    "data": {
      "title": "مقدمه‌ای بر معماری تمیز",
      "body": "<p>معماری تمیز به ما کمک می‌کند تا منطق تجاری سیستم را از فریم‌ورک‌ها جدا نگه‌داریم...</p>"
    }
  },
  {
    "id": "blk_3d9a7c54_45fe",
    "type": "media",
    "order": 2,
    "data": {
      "title": "نمودار کلاس‌های اصلی سیستم",
      "media_id": 42,
      "position": "after_text",
      "body": "<p>این نمودار روابط بین لایه سرویس و مدل‌های پایگاه داده را نشان می‌دهد.</p>"
    }
  }
]
```

---

## ۳. طرح‌واره جیسون (JSON Schema Design)

برای تضمین یکپارچگی داده‌ها (Data Integrity) در پایگاه داده PostgreSQL، یک **JSON Schema** استاندارد تعریف می‌شود که هر ساختار ارسالی به فیلد `content_blocks` باید کاملاً با آن مطابقت داشته باشد.

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BlockBasedContentSchema",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "type", "order", "data"],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^blk_[a-f0-9]{8}_[a-f0-9]{4}$",
        "description": "شناسه منحصربه‌فرد هر بلاک تولید شده در سمت کلاینت یا سرور"
      },
      "type": {
        "type": "string",
        "enum": ["text", "media"],
        "description": "نوع بلاک"
      },
      "order": {
        "type": "integer",
        "minimum": 1,
        "description": "ترتیب نمایش بلاک"
      },
      "data": {
        "type": "object",
        "description": "محتوای اختصاصی بلاک بر اساس نوع آن"
      }
    },
    "allOf": [
      {
        "if": {
          "properties": { "type": { "const": "text" } }
        },
        "then": {
          "properties": {
            "data": {
              "type": "object",
              "required": ["title", "body"],
              "properties": {
                "title": { "type": "string", "maxLength": 255 },
                "body": { "type": "string" }
              }
            }
          }
        }
      },
      {
        "if": {
          "properties": { "type": { "const": "media" } }
        },
        "then": {
          "properties": {
            "data": {
              "type": "object",
              "required": ["media_id", "position"],
              "properties": {
                "title": { "type": "string", "maxLength": 255 },
                "body": { "type": "string" },
                "media_id": { "type": "integer", "minimum": 1 },
                "position": { "type": "string", "enum": ["before_text", "after_text"] }
              }
            }
          }
        }
      }
    ]
  }
}
```

---

## ۴. استراتژی اعتبارسنجی (Validation Strategy)

اعتبارسنجی در چند سطح جداگانه (Multi-tier Validation) برای تضمین امنیت و پایداری داده انجام می‌پذیرد:

```
[کلاینت] ----> [اعتبارسنجی لایه سریالایزر (بررسی ساختار کلی)]
                             |
                             v
               [بررسی با طرح‌واره JSON Schema]
                             |
                             v
               [اعتبارسنجی ارجاعات رسانه‌ها (Media Integrity)]
                             |
                             v
               [اعتبارسنجی منطق کسب‌وکار (بلاک‌های خالی، تکراری، مرتب‌سازی)]
```

### ۱. اعتبارسنجی رسانه (Media Validation)
- تمام فیلدهای `media_id` موجود در بلاک‌ها استخراج می‌شوند.
- سیستم با یک کوئری بهینه به جدول `medias.Media` بررسی می‌کند که آیا رسانه‌های ارسالی در سیستم وجود دارند یا خیر.
- هرگونه مغایرت منجر به بروز خطای `Validation Error` با جزئیات منحصربه‌فرد بلاک فاقد رسانه می‌شود.

### ۲. اعتبارسنجی چیدمان و موقعیت (Position Validation)
- فیلد `position` در بلاک‌های رسانه، به مقادیر مجاز اعلام‌شده یعنی `before_text` یا `after_text` محدود می‌شود.

### ۳. اعتبارسنجی ترتیب نمایش (Order Validation)
- فیلد `order` باید از ۱ آغاز شده و به صورت متوالی پیش برود.
- در صورتی که مقادیر غیرمتوالی ارسال شود، لایه سرویس در بک‌اِند وظیفه مرتب‌سازی مجدد و "تسطیح چیدمان" (Normalization) را بر عهده می‌گیرد.

### ۴. تشخیص همپوشانی و بلاک‌های تکراری (Duplicate Detection)
- شناسه‌های منحصربه‌فرد هر بلاک (`id`) بررسی شده تا مطمئن شویم هیچ دو بلاکی با شناسه یکسان در یک مقاله ارسال نشده است.

### ۵. تشخیص بلاک‌های توخالی (Empty Block Detection)
- بلاک‌هایی که فاقد هرگونه متن غنی (`body`) بوده و رسانه‌ای نیز برای آن‌ها تنظیم نشده باشد، نامعتبر شناسایی شده و فیلتر می‌شوند.

### ۶. اعتبارسنجی ساختار نامعتبر (Invalid Structure Detection)
- در صورت وجود فیلدهای اضافی خارج از محدوده Schema، بلافاصله خطا صادر شده و تراکنش متوقف می‌گردد.

---

## ۵. معماری سریالایزر (Serializer Architecture)

طراحی لایه مبدل و سریالایزرها به شکل کاملاً ماژولار و بر پایه **Composition** طراحی می‌شود تا از تکرار کد جلوگیری شده و انعطاف‌پذیری حفظ گردد.

```
                  +--------------------------------+
                  |  ArticleCreateUpdateSerializer |
                  +--------------------------------+
                                  |
                                  v
                  +--------------------------------+
                  |    ArticleTranslationSerializer|
                  +--------------------------------+
                                  |
               content_blocks     v
                  +--------------------------------+
                  |      JSONBlockFieldSerializer  |
                  +--------------------------------+
                       /                      \
                      v                        v
        +-------------------------+      +-------------------------+
        |   TextBlockSerializer   |      |   MediaBlockSerializer  |
        +-------------------------+      +-------------------------+
```

### ۱. ساختار کامپوننت‌ها (Nested Serializers)
- **`JSONBlockFieldSerializer`**: این سریالایزر سفارشی بر روی فیلد `content_blocks` اعمال می‌شود. این سریالایزر وظیفه روتینگ بلاک‌ها را بر عهده دارد؛ بر اساس فیلد `type` مشخص می‌کند که محتوای درونی `data` باید با کدام سریالایزر فرعی (مثلاً `TextBlockSerializer` یا `MediaBlockSerializer`) اعتبارسنجی شود.

### ۲. فرآیند اعتبارسنجی (Validation Flow)
- متد `to_internal_value` در `JSONBlockFieldSerializer` فعال شده و ابتدا با پکیج `jsonschema` طرح‌واره را بررسی می‌کند.
- پس از تایید اولیه، بلاک‌ها را تفکیک کرده و فرآیند اعتبارسنجی تخصصی (مانند بررسی وجود رسانه‌ها و پاکسازی متون Rich Text از کدهای مخرب) را پیاده‌سازی می‌کند.

### ۳. انبساط اطلاعات رسانه (Media Expansion / Representation Flow)
- متد `to_representation` سریالایزر اصلی بازنویسی می‌گردد.
- شناسه‌های `media_id` موجود در کل بدنه بلاک‌ها در قالب یک لیست متمرکز جمع‌آوری شده و در یک کوئری واحد (Single Query Fetch) تمام اشیای `Media` متناظر خوانده می‌شوند.
- سپس اطلاعات کامل شیء رسانه (از جمله آدرس عمومی، ابعاد و تگ آلت) با استفاده از `MediaDetailSerializer` منبسط شده و درون کلید `media` در بلاک‌های مربوطه تزریق می‌گردد تا کلاینت فرانت‌اند نیازی به ارسال درخواست جداگانه برای دریافت مشخصات رسانه‌ها نداشته باشد.

---

## ۶. طراحی لایه سرویس (Service Layer Design)

مطابق با اصول **Clean Architecture**، منطق اصلی پردازش‌های تجاری خارج از محدوده سریالایزرها و کنترلرها، در لایه مجزای `Service` پیاده‌سازی می‌شود.

```python
class ArticleBlockService:
    @staticmethod
    def validate_blocks_schema(blocks_data: list) -> None:
        """
        اعتبارسنجی کل بلاک‌ها با استفاده از پکیج jsonschema در پایتون.
        """
        pass

    @staticmethod
    def normalize_blocks_order(blocks_data: list) -> list:
        """
        مرتب‌سازی و کالیبره کردن فیلد order بلاک‌ها از عدد ۱ تا N برای رفع ناهماهنگی سمت کلاینت.
        """
        pass

    @staticmethod
    def sync_block_media_relationships(article: Article, blocks_data: list) -> None:
        """
        استخراج تمام media_idها از بلاک‌ها و همگام‌سازی آن‌ها با جدول واسط `ArticleMedia`
        جهت ردیابی دقیق (Media Reference Tracking) و جلوگیری از حذف تصادفی رسانه‌های فعال.
        """
        pass

    @staticmethod
    def sanitize_html_content(blocks_data: list) -> list:
        """
        پاکسازی خودکار تمام تگ‌های غیرمجاز و کدهای مخرب جاوا اسکریپت در تگ‌های HTML
        با بهره‌گیری از کتابخانه BeautifulSoup در بک‌اِند.
        """
        pass

    @staticmethod
    def cleanup_orphaned_media(article: Article) -> None:
        """
        پاکسازی ارجاعات یتیم (Orphaned Media) در جدول واسط پس از ویرایش و حذف بلاک‌ها.
        """
        pass
```

---

## ۷. توافق‌نامه ای‌پی‌آی (API Contract)

### ۱. ارسال درخواست ایجاد/ویرایش (POST / PUT)
- **آدرس (Endpoint):** `/api/articles/` یا `/api/articles/{slug}/`
- **نمونه بدنه درخواست (Payload JSON):**

```json
{
  "language_code": "fa",
  "title": "راهنمای جامع معماری سیستم",
  "excerpt": "معماری بلاک‌محور و انعطاف‌پذیری فوق‌العاده در CMSهای سازمانی",
  "status": "draft",
  "content_blocks": [
    {
      "id": "blk_a1b2c3d4_1001",
      "type": "text",
      "order": 1,
      "data": {
        "title": "مقدمه",
        "body": "<p>این مقاله به تشریح سیستم نوین ما می‌پردازد.</p>"
      }
    },
    {
      "id": "blk_e5f6g7h8_1002",
      "type": "media",
      "order": 2,
      "data": {
        "title": "کتابخانه رسانه",
        "body": "<p>توضیحات تکمیلی پیرامون ساختار بهینه‌سازی تصاویر.</p>",
        "media_id": 14,
        "position": "after_text"
      }
    }
  ]
}
```

### ۲. ساختار پاسخ موفق (Response Payload)

```json
{
  "status": "success",
  "data": {
    "id": 127,
    "language_code": "fa",
    "title": "راهنمای جامع معماری سیستم",
    "slug": "راهنمای-جامع-معماری-سیستم",
    "content_blocks": [
      {
        "id": "blk_a1b2c3d4_1001",
        "type": "text",
        "order": 1,
        "data": {
          "title": "مقدمه",
          "body": "<p>این مقاله به تشریح سیستم نوین ما می‌پردازد.</p>"
        }
      },
      {
        "id": "blk_e5f6g7h8_1002",
        "type": "media",
        "order": 2,
        "data": {
          "title": "کتابخانه رسانه",
          "body": "<p>توضیحات تکمیلی پیرامون ساختار بهینه‌سازی تصاویر.</p>",
          "media_id": 14,
          "position": "after_text",
          "media": {
            "id": 14,
            "url": "https://cdn.example.com/medias/2026/07/clean-arch.avif",
            "type": "image",
            "mime": "image/avif",
            "width": 1200,
            "height": 630,
            "alt_text": "ساختار معماری تمیز پروژه",
            "title": "Clean Architecture Diagram"
          }
        }
      }
    ]
  },
  "messagesList": []
}
```

### ۳. ساختار خطاهای اعتبارسنجی (Validation Error Responses)
در تطابق کامل با سیاست خطاهای استاندارد شده پروژه، هرگونه خطای تجاری در ساختار `messagesList` تحویل داده می‌شود:

```json
{
  "status": "error",
  "data": null,
  "messagesList": [
    {
      "field": "content_blocks[1].data.media_id",
      "message": "رسانه‌ای با شناسه ۱۴ در سیستم وجود ندارد و ارجاع نامعتبر است."
    },
    {
      "field": "content_blocks[0].order",
      "message": "ترتیب بلاک‌ها هم‌پوشانی دارد یا متوالی نیست."
    }
  ]
}
```

---

## ۸. طراحی پنل مدیریت (Admin Panel Design)

یکی از بخش‌های کلیدی سیستم، ارائه یک رابط کاربری ارگونومیک و قدرتمند در Django Admin بدون پیچیدگی‌های دیتابیس است. با توجه به استفاده از پکیج‌های مدرن مانند **Django Unfold**، طراحی ادمین بر اساس اصول زیر استوار خواهد بود:

```
+-----------------------------------------------------------+
| [مدیریت مقاله] - بخش ترجمه فارسی                          |
+-----------------------------------------------------------+
|                                                           |
|  +-----------------------------------------------------+  |
|  | [بلاک ۱] - نوع: متنی (Text Block)                   |  |
|  | عنوان بلاک: [مقدمه سیستم                           ] |  |
|  | متن غنی:                                            |  |
|  | [ [CKEditor 5 Editor Instance]                    ] |  |
|  +-----------------------------------------------------+  |
|                                                           |
|       ^                                                   |
|  [Drag & Drop - تغییر ترتیب نمایش به صورت بصری]            |
|       v                                                   |
|                                                           |
|  +-----------------------------------------------------+  |
|  | [بلاک ۲] - نوع: رسانه‌ای (Media Block)               |  |
|  | رسانه: [ انتخاب فایل از کتابخانه رسانه‌ها (Select2)  ] |  |
|  | جایگاه رسانه: ( ) قبل از متن  (*) بعد از متن        |  |
|  +-----------------------------------------------------+  |
|                                                           |
|  [+ افزودن بلاک جدید] -> [متنی] | [تصویری] | [ویدیو] ...   |
+-----------------------------------------------------------+
```

### ویژگی‌های پنل مدیریت پیشنهادی:
1. **استفاده از JSON Widgetهای هوشمند:** تعبیه یک ویجت اختصاصی جاوا اسکریپت (به عنوان مثال بر پایه Alpine.js یا Vue.js درون ساختار Django Admin) که فیلد متنی پنهانِ JSON را به یک سازنده بصری بلاک‌محور تبدیل می‌کند.
2. **قابلیت کشیدن و رها کردن (Drag & Drop Reordering):** جابه‌جایی چیدمان بلاک‌ها به صورت کاملاً بصری در صفحه ادمین که پس از جابه‌جایی، فیلد `order` بلاک‌ها را در قالب JSON بلافاصله به‌روزرسانی می‌کند.
3. **جستجوی رسانه با Select2 پویا:** انتخاب رسانه در بلاک‌ها از طریق ویجت‌های مبتنی بر Select2 انجام می‌شود که دیتای رسانه‌ها را به صورت Ajax از آدرس `/api/media/` جستجو و لود می‌کند.
4. **پیش‌نمایش زنده (Live Content Preview):** امکان پیش‌نمایش درجا بدون نیاز به ذخیره‌سازی نهایی رکورد.

---

## ۹. یکپارچگی با فرانت‌اند (Frontend Integration)

برنامه‌های مدرن مبتنی بر Next.js یا React به راحتی این ساختار غنی و سازمان‌یافته را مصرف و رندر می‌کنند.

### ۱. استراتژی رندرینگ (Rendering Strategy)
- با بهره‌گیری از **Server-Side Rendering (SSR)** یا **Incremental Static Regeneration (ISR)** در Next.js، ساختار بلاک‌ها در سرور پارس شده و به صورت کامپوننت‌های کاملاً بهینه و استاتیک تحویل موتورهای جستجو داده می‌شود.

### ۲. نقشه‌برداری بلاک‌ها (Block Mapping Pattern)
فرانت‌اند یک هوک یا کامپوننت توزیع‌کننده مرکزی (`BlockRenderer`) دارد که ساختار درختی را پیمایش کرده و هر نوع را به کامپوننت ری‌اکتی متناظر متصل می‌کند:

```typescript
// Components Mapping Dictionary
const BLOCK_COMPONENTS: Record<string, React.FC<any>> = {
  text: TextBlockComponent,
  media: MediaBlockComponent,
  quote: QuoteBlockComponent,
};

export const BlockRenderer: React.FC<{ blocks: Block[] }> = ({ blocks }) => {
  return (
    <>
      {blocks
        .sort((a, b) => a.order - b.order)
        .map((block) => {
          const Component = BLOCK_COMPONENTS[block.type];
          if (!Component) return null;
          return <Component key={block.id} data={block.data} />;
        })}
    </>
  );
};
```

### ۳. رندرینگ رسانه‌ها و Lazy Loading
- کامپوننت `MediaBlockComponent` از قابلیت‌های بهینه‌سازی تصویر Next.js (`<Image />`) بهره گرفته و با توجه به ابعاد ارسالی از سمت بک‌اِند (`width` و `height`) و فرمت نوین فایل (`AVIF`)، تصاویر را با کیفیت بالا و لود تنبل (Lazy Loading) نمایش می‌دهد.

---

## ۱۰. تحلیل عملکرد (Performance Analysis)

یکی از جنبه‌های کلیدی در سیستم‌های مقیاس بزرگ، بهینه‌سازی زیرساخت داده و کشینگ است.

### ۱. بارگذاری بهینه پایگاه داده (Database Query Optimization)
- برخلاف حالت رابطه‌ای که نیاز به اجرای کوئری‌های تودرتو یا `SELECT ... LEFT JOIN` دارد، کل ساختار بلاک‌ها درون فیلد بومی `JSONB` در PostgreSQL ذخیره می‌شود.
- در پایگاه داده PostgreSQL، این فیلد به صورت فشرده باینری ذخیره می‌گردد و لود شدن آن به سرعت خواندن یک فیلد متنی معمولی است.
- برای سناریوهایی که نیاز به جستجوی بلاک خاصی وجود دارد، می‌توان از **شاخص‌های تخصصی GIN (Generalized Inverted Index)** روی ستون JSON استفاده کرد:
  ```sql
  CREATE INDEX idx_article_translation_blocks ON posts_articletranslation USING gin (content_blocks);
  ```

### ۲. هزینه پردازشی سریالایزر (Serialization Overhead)
- برای کاهش بار پردازشی CPU ناشی از اجرای حلقه روی بلاک‌ها و انبساط رسانه‌ها، تمام داده‌های به دست آمده از متد `to_representation` بلافاصله وارد لایه کشینگ می‌شوند.

### ۳. استراتژی کشینگ پیشرفته (Cache-Aside & SWR integration)
- در هماهنگی کامل با **زیرسیستم کشینگ سازمانی** مستقر در پروژه:
  - پاسخ نهایی هر مقاله با تمام لایه‌های انبساط یافته بلاک‌هایش در Redis با یک کلید پویا (مانند `article_detail_blocks:fa:<slug>`) کش می‌شود.
  - با اعمال تغییر یا ویرایش در ترجمه‌ها، سیگنال جنگو بلافاصله تسک Celery را برای نوسازی و پیش‌گرم‌سازی فعال کش (Selective Warmup) آغاز می‌کند تا هیچ تاخیری برای بازدیدکننده بعدی ایجاد نشود.

---

## ۱۱. تحلیل امنیت (Security Analysis)

معماری بلاک‌محور باید در برابر نفوذ و حملات اینترنتی کاملاً واکسینه باشد.

```
[ورودی کلاینت] ---> [BeautifulSoup HTML Sanitizer (حذف اسکریپت‌ها و استایل‌های غیرمجاز)]
                       |
                       v
             [احراز هویت و سطوح دسترسی (JWT / Static API Key)]
                       |
                       v
             [بررسی ارجاعات رسانه‌ها (بررسی شناسه و دسترسی کاربر آپلود کننده)]
```

### ۱. جلوگیری از حملات تزریق کد (XSS Prevention)
- داده‌های ورودی در فیلدهای `body` هر بلاک از فیلتر بهینه‌سازی و ضدعفونی‌کننده شدید عبور می‌کنند.
- استفاده از کتابخانه **BeautifulSoup** در لایه لایف‌سایکل ذخیره‌سازی، تگ‌های آسیب‌پذیر نظیر `<script>`, `<iframe>` و اتریبیوت‌های خطرناک مانند `onload` را به کلی حذف و پاکسازی (Sanitize) می‌کند.

### ۲. اعتبارسنجی ارجاعات فایل‌ها (Broken & Invalid Reference Security)
- برای جلوگیری از جعل پیوندها یا استفاده غیرمجاز از رسانه‌های مخفی دیگر کاربران، سیستم وضعیت و دسترسی‌های فیزیکی کاربر درخواست‌دهنده نسبت به `media_id`های ارسالی در بلاک‌ها را بررسی و ممیزی می‌کند.

---

## ۱۲. تحلیل سئو (SEO Analysis)

تاثیر ساختار بلاک‌محور بر روی ربات‌های موتور جستجو بی‌نظیر است:

1. **ساختار هدرها (Heading Structure Hierarchy):** فرانت‌اند به راحتی می‌تواند تگ‌های Heading (`h1`, `h2`, `h3`) بلاک‌ها را به صورت کاملاً ساختاریافته تحلیل کرده و نقشه درختی وب‌سایت (Outline) را دقیق و بدون خطا شکل دهد.
2. **متادیتاهای خودکار تصاویر (Image Alt Metas):** از آنجا که تصاویر از سیستم متمرکز رسانه واکشی می‌شوند، تگ `alt_text` همواره از دیتابیس خوانده شده و موتورهای جستجو دسترسی کاملی به شرح تصاویر خواهند داشت.
3. **تولید داده‌های ساختاریافته (Structured Data / Schema.org):** با تجزیه ساده بلاک‌ها در لایه سرور فرانت‌اند (Next.js)، می‌توان کدهای استاندارد JSON-LD (نظیر `ArticleSchema` و `HowToSchema`) را برای بهبود رتبه در نتایج گوگل به صورت خودکار تولید کرد.

---

## ۱۳. قابلیت توسعه در آینده (Future Extensibility)

یکی از درخشان‌ترین ویژگی‌های معماری مبتنی بر `JSONField` این است که در آینده برای اضافه کردن هر نوع بلاک جدید، **هیچ نیازی به نوشتن Migration دیتابیس یا اجرای دستورات مخرب روی پایگاه داده عملیاتی نیست.**

به عنوان مثال، در صورت نیاز به افزودن بلاک‌های پیشرفته، تنها کافیست:
1. کلاس سریالایزر جدید را در بک‌اِند تعریف کنیم.
2. قوانین اعتبارسنجی آن را به JSON Schema بیفزاییم.
3. کامپوننت نمایشگر آن را در ری‌اکت/Next.js بسازیم.

### سناریوهای توسعه بلاک‌ها در آینده (Future Block Schemas):

- **بلاک گالری تصاویر (Gallery Block):**
  ```json
  {
    "type": "gallery",
    "data": {
      "images": [
        { "media_id": 102, "caption": "تصویر نمای کلی" },
        { "media_id": 103, "caption": "تصویر جزئیات داخلی" }
      ]
    }
  }
  ```
- **بلاک ویدیوی جریانی (Video Streaming Block):**
  ```json
  {
    "type": "video",
    "data": {
      "media_id": 85,
      "autoplay": false,
      "controls": true
    }
  }
  ```
- **بلاک سوالات متداول (FAQ Block):**
  ```json
  {
    "type": "faq",
    "data": {
      "items": [
        { "question": "چگونه ثبت نام کنم؟", "answer": "از منوی بالای صفحه دکمه ثبت نام را بزنید." }
      ]
    }
  }
  ```

---

## ۱۴. مخاطرات و محدودیت‌ها (Risks & Limitations)

اگرچه معماری `JSONField` انتخاب اول CMSهای مطرح جهانی در سطح اینترپرایز است، اما شناخت چالش‌های آن و داشتن راهکارهای پیشگیرانه ضرورت دارد:

| ردیف | نام مخاطره / چالش (Risk) | شرح چالش | راهکار پیشنهادی فنی (Mitigation Strategy) |
| :--- | :--- | :--- | :--- |
| ۱ | **یکپارچگی ارجاعات (Referential Integrity)** | پایگاه داده به صورت پیش‌فرض نمی‌تواند بین فیلد درونی کلید JSON و فیلد اصلی کلید خارجی در جدول رسانه‌ها (`medias.Media`) قفل سخت‌افزاری (Foreign Key Constraint) ایجاد کند. اگر رسانه‌ای حذف شود، ممکن است ارجاع آن در بلاک خراب شود. | **استفاده از سیگنال‌ها و جدول واسط:** لایه سرویس در هر ایجاد/به‌روزرسانی، بلاک‌ها را اسکن کرده و رکوردهای جدول واسط `ArticleMedia` را بازنویسی می‌کند. همچنین پیش از حذف هر رسانه در مدل `Media`، بررسی می‌شود که آیا شناسه آن در جدول واسط یا کدهای JSON ردیابی شده است یا خیر (جلوگیری اتمیک از حذف رسانه‌های فعال). |
| ۲ | **اعتبارسنجی ساختار داده (Data Validation)** | خطا در نوشتن کلاینت ممکن است ساختارهای درختی نامعتبر را وارد پایگاه داده کند. | **اعتبارسنجی دو مرحله‌ای با JSON Schema:** ادغام سخت‌گیرانه پکیج `jsonschema` پایتون در متد اعتبارسنجی سریالایزرها پیش از ذخیره‌سازی نهایی تراکنش دیتابیس. |
| ۳ | **گزارش‌گیری پیچیده (SQL Reporting)** | کوئری‌های فیلترینگ پیچیده روی بخش‌های داخلی داده‌های جیسون نسبت به جداول رابطه‌ای سخت‌تر است. | **موتور جستجوی فرعی یا استفاده از کدهای تخصصی PostgreSQL:** پایگاه داده PostgreSQL دارای توابع بی‌نظیری نظیر `jsonb_to_recordset` است که به راحتی ساختار JSON را در کسری از میلی‌ثانیه به رکوردهای موقت SQL تبدیل می‌کند. همچنین برای گزارش‌گیری‌های کلان، از دیتای لود شده در الاستیک‌سرچ یا ایندکس‌های تخصصی استفاده خواهد شد. |
| ۴ | **جستجوی متن (Searchability)** | جستجوی متنی روی بلاک‌های تو در تو ممکن است زمان‌بر باشد. | **فیلد جستجوی تخت پیش‌گرم‌شده:** یک ستون متنی ساده و پنهان در لایه دیتابیس به نام `search_vector` نگهداری می‌شود که در زمان ثبت مقاله، تمام متون بلاک‌ها را استخراج و در قالب یک متن تمیز و تخت در خود ذخیره می‌کند تا جستجوی متنی با سرعت حداکثری و بدون بارگذاری کل بلاک‌ها انجام شود. |

---

## ۱۵. جدول مقایسه جامع معماری‌ها (Comparison)

برای ارزیابی همه‌جانبه، مقایسه‌ای عمیق میان دو رویکرد اصلی مدیریت بلاک‌ها ارائه شده است:

| معیار مقایسه (Metric) | معماری پیشنهادی: JSONField | معماری جایگزین: Relational Sections |
| :--- | :--- | :--- |
| **کارایی خواندن (Read Performance)** | **فوق‌العاده سریع (Excellent):** کل مقاله با تمام بلاک‌هایش در یک کوئری سریع و مستقیم لود می‌شود. نیازی به Joinهای سنگین نیست. | **متوسط (Moderate):** با افزایش تعداد بخش‌ها، نیاز به الحاق (Join) چندباره جداول است که بار دیتابیس را افزایش می‌دهد. |
| **قابلیت نگهداری (Maintainability)** | **بسیار آسان:** تغییر ساختار بلاک‌ها نیازی به ایجاد و اجرای فایل‌های مهاجرت (Migrations) پیچیده دیتابیس ندارد. | **دشوار:** کوچکترین تغییر در نوع بلاک‌ها نیازمند ویرایش فیلدها و انجام تراکنش‌های مهاجرت طولانی است. |
| **انعطاف‌پذیری توسعه (Flexibility)** | **بی‌نهایت:** به راحتی می‌توان انواع بلاک جدید (Quotes, Code, Embeds) بدون دخالت در دیتابیس ایجاد کرد. | **محدود:** نیاز به ساخت جداول جدید به عنوان جدول‌های واسط یا استفاده از الگوهای پیچیده ارث‌بری مدل‌ها دارد. |
| **سازگاری چندزبانی (Multilingual)** | **طبیعی و هماهنگ:** ساختار درختی بلاک‌ها در مدل ترجمه بومی‌سازی شده قرار گرفته و به ازای هر زبان کاملاً تفکیک شده است. | **پیچیده:** نیاز به جدول‌های میانی متعدد برای ترجمه تک‌تک فیلدها به صورت چندسطحی دارد. |
| **تجربه توسعه‌دهنده (Dev Experience)** | **بسیار مطلوب:** هماهنگی بالا با ساختار خروجی JSON و کامپوننت‌های فرانت‌اند. | **پیچیده:** نیاز به سریالایزرهای متعدد پلی‌مورفیک (Polymorphic Serializers) دارد. |
| **تجربه کاربری ادمین (Admin UX)** | **بسیار عالی (با ویجت جاوا اسکریپت):** نویسنده در یک صفحه منسجم محتوا را مدیریت می‌کند. | **متوسط:** نیاز به استفاده از Inlines جنگو دارد که در ویرایش‌های تودرتوی بزرگ، سنگین و خسته‌کننده می‌شود. |

---

## ۱۶. جمع‌بندی و پیشنهاد نهایی (Final Recommendation)

پس از بررسی موشکافانه ساختار فعلی پروژه، زیرسیستم‌های پیشرفته کشینگ و پشتیبان‌گیری، کتابخانه مرکزی رسانه و اهداف بلندمدت پلتفرم، **معماری مبتنی بر JSONField در مدل `ArticleTranslation` به عنوان معماری استاندارد و منتخب تایید می‌گردد.**

### دلایل کلیدی تایید نهایی:
1. **سازگاری ۱۰۰ درصدی با سیستم فعلی:** این روش هیچ تغییری در ساختار اصلی روابط چندزبانی ایجاد نکرده و به راحتی با پیاده‌سازی متد همگام‌سازی `sync_article_media` و جدول واسط `ArticleMedia` یکپارچه می‌شود.
2. **کاهش هزینه‌های سرور (SLA & P95 Latency):** با تک‌کوئری شدن فرآیند خواندن مقاله‌ها و تلفیق با الگوی SWR در زیرسیستم کش، زمان پاسخ‌دهی لایه وب حتی در لودهای بسیار سنگین پایدار خواهد ماند.
3. **آماده برای فرانت‌اند مدرن:** فرانت‌اندهایی مثل Next.js به طور کامل برای مصرف الگوهای درختی JSON طراحی شده‌اند و رندر کردن این ساختار تجربه توسعه درخشانی به همراه دارد.

این سند به عنوان مرجع فنی پیاده‌سازی فاز آینده پروژه پیشنهاد می‌گردد و مبنای توسعه کدهای بلاک‌محور خواهد بود.

---
**پایان سند طراحی فنی معماری محتوای بلاک‌محور پویا**
